## Mudança
Adicionar `"Sub-9"` à lista de categorias disponíveis no formulário de criação de peneiras.

### Arquivo
`src/routes/peneiras.criar.tsx` — adicionar `"Sub-9"` como primeiro item da constante `CATEGORIAS` (antes de `"Sub-11"`), mantendo a ordem crescente por idade.

Nenhuma outra alteração é necessária — o banco já aceita qualquer string no array `categorias`.