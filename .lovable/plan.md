O ícone selecionado no preview é o `<Sparkles>` dentro do badge "Plataforma oficial" na primeira section da landing page (`src/routes/index.tsx`).

**Alteração:** Remover o componente `<Sparkles className="h-3.5 w-3.5" />` e o `gap-2` do span, mantendo apenas o texto "Plataforma oficial".